{
  "name": "casino-platform",
  "version": "1.0.0",
  "description": "Professional simulation casino platform with Node.js, React, and SQLite",
  "private": true,
  "repository": {
    "type": "git",
    "url": "https://github.com/tristan5983/casino.git"
  },
  "keywords": [
    "casino",
    "slots",
    "simulation",
    "igaming",
    "react",
    "nodejs",
    "express",
    "vite"
  ],
  "author": "Development Team",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  },
  "scripts": {
    "setup": "bash setup.sh",
    "dev": "echo 'Run backend and frontend in separate terminals' && echo 'Terminal 1: cd backend && npm run dev' && echo 'Terminal 2: cd frontend && npm run dev'",
    "start": "npm --prefix backend start",
    "build": "npm --prefix frontend run build",
    "install-all": "npm --prefix backend install && npm --prefix frontend install"
  },
  "workspaces": [
    "backend",
    "frontend"
  ]
}
